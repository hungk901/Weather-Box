CREATE TABLE IF NOT EXISTS `location` (
   `username`        VARCHAR(20)  NOT NULL,
   `boxid`            VARCHAR(5)  NOT NULL,
   `cityname`        VARCHAR(30)  NOT NULL,
   `countryname`     VARCHAR(30)  NOT NULL
) ENGINE=MyISAM DEFAULT CHARSET=utf8;
